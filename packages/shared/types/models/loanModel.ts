export interface ProductProps {
  interest: number;
  maxAmount: string;
  productCode: string;
  productName: string;
}

export interface PurposeProps {
  name: string;
  code: string;
  product: string;
  template: null;
}
